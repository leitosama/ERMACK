{
  "image": "mcr.microsoft.com/devcontainers/python:3",
  "features": {
  },
  "forwardPorts": [ 8000 ],
  "postCreateCommand": "pip install -r requirements.txt && python main.py mkdocs -a -i && cd build && python -m mkdocs serve -a 127.0.0.1:8000"
}
